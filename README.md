### How to run:

1. Install scrapy

- direct install
    ```
    pip install scrapy
    ```

- use conda.
    ```
    conda install -c conda-forge scrapy
    ```  

2. Build project
    ```
    scrapy crawl <spider_name>
    ```
